package org.tyndalebt.spadv.controller.dramatization

    // Not quite sure if this class is needed at all.
    // Seems like the code for the fragment_dramatization.xml is somewhere else. (Maybe something voice studio related)

class DramatizationFrag {
}